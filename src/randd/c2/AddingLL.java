package randd.c2;

import common.LinkedList;
import java.math.*;

public class AddingLL {

	/*
	 public static LinkedList addLL(LinkedList a, LinkedList b) {
		LinkedList out = new LinkedList();
		
		for (int i =0; i < Math.max(a.size, b.size); i++ ) {
			out.addFirst(a.getByIndex(i) + b.getByIndex(i) % 10);
		
		}
	}
	*/
}
